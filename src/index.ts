import { camelCase } from 'lodash';
import { subArray } from './parsingEngine/parsingServiceUtils';

const age : number = 43
console.log(age)
console.log(camelCase("myNameIsEden"));

// const source : number[] = [1,2,3,4,5,6];
// const res = subArray(source,4,1);
// console.log("that it ");